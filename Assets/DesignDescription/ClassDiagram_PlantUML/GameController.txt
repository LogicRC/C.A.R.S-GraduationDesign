@startuml
' 强制声明类图类型并启用严格模式
!pragma useVerticalIf on
!pragma strict

' 定义命名空间层级
namespace CARS {
  class GameController {
    - {static} GameController instance
    + string inputName
    - Text txtShow
    - string pathKeyPoints
    - string pathRoads
    + void Awake()
    + void Start()
    + void ShowMessage(string)
    - IEnumerator EndShowMessage()
    + void SaveKeyPoints(string[])
    + List<string> LoadKeyPoins()
    + void SaveRoads(string[])
    + List<string> LoadRoads()
    - void SaveStringArray(string[], string)
    - List<string> LoadStringList(string)
  }
}

' Unity 核心组件
namespace UnityEngine.UI {
  class Text {
    + string text
    + GameObject gameObject
  }
}

namespace UnityEngine {
  class Application {
    + {static} string persistentDataPath
  }
  class Debug {
    + {static} void Log(string)
  }
  class MonoBehaviour {
    # void Awake()
    # void Start()
  }
}

' 文件系统组件
namespace System.IO {
  class StreamWriter {
    + void WriteLine(string)
  }
  class StreamReader {
    + string ReadLine()
  }
}

' 继承关系
CARS.GameController --|> UnityEngine.MonoBehaviour

' 依赖关系
CARS.GameController --> UnityEngine.UI.Text : 1. controls
CARS.GameController --> System.IO.StreamWriter : 2. writes
CARS.GameController --> System.IO.StreamReader : 3. reads
CARS.GameController --> UnityEngine.Application : 4. accesses

' 单例模式标注
note top of CARS.GameController
  <<Singleton>>
  Implementation method:
  1. Static instance control
  2. DontDestroyOnLoad
  3. Automatically destroy duplicate instances
end note

' 生命周期方法说明
note left of CARS.GameController::Awake
  Unity lifecycle:
  - Initialization singleton
  - Set persistent path
  - Ensure uniqueness
end note

' 文件操作流程
note right of CARS.GameController::SaveStringArray
  <<File Operation>>
  Secure Writing Process:
  1. Using StreamWriter
  2. try-catch exception capture
  3. using Automatically release resources
end note

@enduml